version: "3.8"

services:
  billing-suite:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: billing-suite
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
